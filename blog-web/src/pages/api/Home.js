import Link from 'next/link';
import Header from '../components/Header'

export default function Home() {
    return (
        <main className={`w-screen h-auto m-0 px-96`}>
            <Header />
            Baller
        </main>
    );
}